using UnityEngine;

namespace OctoberStudio.DI
{
    /// <summary>
    /// MonoBehaviour updater for SceneLoaderService.
    /// Provides a Unity context for running coroutines in the pure service.
    /// </summary>
    public class SceneLoaderSystemUpdater : MonoBehaviour
    {
        // This class exists solely to provide MonoBehaviour context for coroutines
        // No additional functionality needed - the service handles all logic
    }
}