namespace OctoberStudio.Pool
{
    public enum PoolType
    {
        // Common pool types that might be used in the game
        Enemy,
        Projectile,
        Drop,
        Effect,
        UI,
        Audio,
        Custom
    }
}